# TikCanvas Agent Notes

- 默认使用中文回复。
- Demo 内容必须只引用仓库中真实存在的本地素材，不要虚构视频标题、作者、封面或播放源。
- 当前真实视频源只有 `videos/video-1.mp4`、`videos/video-2.mp4`、`videos/video-3.mp4`。
- 不要占用 `4173` 端口；本地验证如需启动服务，使用其他临时端口。
- 修改后优先运行 `npm run check` 验证。
